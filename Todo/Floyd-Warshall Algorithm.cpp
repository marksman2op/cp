// Sky's the limit :)
#include <bits/stdc++.h>
using namespace std;
#define int long long

/*
    Floyd-Warshall Algorithm
    - Used to find shortest path between all pairs of nodes
    - The graph may have negative weight edges, but no negative weight cycles (for then the shortest path is undefined).
    - Can be used to detect the presence of negative cycles.
*/

signed main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    
    int T = 1; 
    // cin >> T; 
    while (T--) {
        
    }
    
    return 0;
}

